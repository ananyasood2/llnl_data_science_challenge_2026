# Registered missing-strut inspection

This run screens the CT intensity along aligned expected strut centrelines. `strut_candidates.csv` is a ranked-review input, not a ground-truth defect label. The STL is intentionally excluded because it is not registered to this CT volume.
