# Viewport NDE Report Generator

Call `create_nde_report` only after a validated viewport context exists. Include
scope, revision, cited tools, and warnings. Reports are derivative artifacts and
must not change source datasets or analysis parameters.

